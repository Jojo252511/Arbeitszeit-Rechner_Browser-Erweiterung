{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],

    /* Modernisierte Modul-Einstellungen, die die alten Optionen ersetzen */
    "module": "ES2022", 
    "moduleResolution": "bundler",

    /* Projekt-Struktur */
    "rootDir": "./scripts",
    "outDir": "./dist", // Wir legen alle kompilierten JS-Dateien direkt in den "dist"-Ordner

    /* Strikte Typ-Prüfung (sehr empfohlen) */
    "strict": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true
  },
  "include": [
    "./scripts/**/*" // Bezieht alle .ts-Dateien aus deinem Skript-Ordner ein
  ]
}